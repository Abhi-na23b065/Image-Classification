# Image-Classification
A PyTorch-based image classification project for the Shift-Guard 10 challenge. It uses a custom residual CNN, data augmentation, label smoothing, SGD optimization, learning-rate scheduling, validation with Macro F1-score, model checkpointing, and 5-round test-time augmentation to generate robust predictions.
